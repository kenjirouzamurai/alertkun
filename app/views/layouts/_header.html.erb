<header class="navbar navbar-dark sticky-top bg-dark flex-md-nowrap p-0 shadow">
  <a href="<%= sites_path %>" class="navbar-brand col-md-3 col-lg-2 me-0 px-3 fs-6">
    <img src="/alertman/images/logo.png" height="32">
  </a>
  <div class="navbar-nav">
    <div class="nav-item text-nowrap">
    <% if user_signed_in? %>
      <%= link_to "マイページ", sites_path, class: "nav-link px-3" %>
    <% else %>
      <%= link_to "ログイン", new_user_session_path, class: "nav-link px-3" %>
    <% end %>
    </div>
  </div>
</header>
